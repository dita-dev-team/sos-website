extends layout

block content
        .login-margins
            form(action="", method="post")
                input(class="form-control text-center", type="text", required, name="username", placeholder="username")
                br
                input(class="form-control text-center", type="password", required, name="password", placeholder="password")
                br
                .align-center
                    input(class="btn btn-primary", type="submit", name="login" value="Login")
